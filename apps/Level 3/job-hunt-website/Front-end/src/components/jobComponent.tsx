//Single job component which will be used on the category jobs as well as other places.

const JobComponent = (props: { jobsList: string[] }) => {
  //
  props.jobsList.forEach((x) => {
    //
  });
};

export default JobComponent;
