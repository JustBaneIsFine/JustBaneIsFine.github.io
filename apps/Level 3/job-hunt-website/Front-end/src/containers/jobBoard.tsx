// container that will hold all the job components..
import React from 'react';

const JobBoard = (props: { jobs }) => {
  // for each props.jobs document
  // return <JobComponent jobs={jobsList} />
};

export default JobBoard;
